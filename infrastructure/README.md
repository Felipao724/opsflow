# Infrastructure

This directory is reserved for services and configuration that support local
OpsFlow development, such as the future PostgreSQL development environment.

Committed files must be reproducible and safe to share. Credentials, generated
state, and machine-specific overrides stay untracked; checked-in example files
may document required values.

This directory does not define a production deployment architecture. Production
infrastructure requires its own explicit scope and decisions.

