# Backend

This directory owns the OpsFlow backend application, its build configuration,
and backend-specific automated tests.

The Spring Boot project will be bootstrapped in a dedicated M0 change. Until
then, this README keeps the intended location visible without adding framework
or business-domain code prematurely.

Cross-project documentation and local supporting services do not belong here;
use `docs/` and `infrastructure/` respectively.

