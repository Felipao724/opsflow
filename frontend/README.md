# Frontend

This directory owns the OpsFlow web application, its build configuration,
static assets, and frontend-specific automated tests.

The Angular project will be bootstrapped in a dedicated M0 change. This
placeholder documents ownership without selecting libraries or introducing UI
functionality ahead of that work.

Backend code and shared local services do not belong here; use `backend/` and
`infrastructure/` respectively.

